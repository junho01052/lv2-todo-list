const initialState = {
    title: "",
    body: ""
}

const addItem = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default addItem;